import 'package:flutter/material.dart';

class BookShelfPage extends StatelessWidget {
  const BookShelfPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Bookshelf',style: TextStyle(
        color: Colors.white,
        fontSize: 30
    ),);
  }
}